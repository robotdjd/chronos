to install:

For Debian/Ubuntu

	sudo apt update
	sudo apt install python3 python3-pip -y
	sudo apt update
	sudo apt install python3 python3-pip -y
	wget https://raw.githubusercontent.com/robotdjd/chronos/main/Chronos_installer.py
	python3 Chronos_installer.py
